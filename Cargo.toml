[workspace]
resolver = "2"
members = [
    "UNIT-01/sandbox",
    "UNIT-01/indexer",
    "UNIT-01/orchestrator",
]

[profile.release]
opt-level = "z"
lto = true
strip = true
codegen-units = 1
panic = "abort"
